unit nextpas.core.text.wildmatch.base;

{$I nextpas.core.settings.inc}

interface

{ L0 placeholder: wildmatch owns no public types yet.
  Kept as base seam so match impl depends only on base/System. }

implementation

end.
