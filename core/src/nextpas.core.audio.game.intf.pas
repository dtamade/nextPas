unit nextpas.core.audio.game.intf;
{$I nextpas.core.settings.inc}
interface
// Placeholder: game has no independent interface per four-piece 按需存在.
// Deprecated aliases live in sfx.intf (see sfx.intf for details).
// This unit is intentionally empty and kept only to avoid breaking old uses;
// new code must use nextpas.core.audio.sfx.intf. Will be removed in next major.
implementation
end.
