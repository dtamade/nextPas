// Auto-generated SSE2 batch kernels (7 operations)
// Generated by simdgen_sse2.py
procedure SSE2ArrayAddF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    addps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    addps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    addps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    addps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    addps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    addss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArrayMulF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    mulps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    mulps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    mulps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    mulps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    mulps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    mulss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArraySubF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    subps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    subps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    subps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    subps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    subps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    subss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArrayDivF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    divps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    divps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    divps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    divps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    divps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    divss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArrayMinF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    minps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    minps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    minps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    minps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    minps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    minss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArrayMaxF32(aSrc1, aSrc2, aDst: PSingle; aCount: SizeUInt);
var
  pS1, pS2, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pS1 := aSrc1; pS2 := aSrc2; pD := aDst;

  asm
    mov rax, pS1
    mov rdx, pS2
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    maxps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    maxps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    maxps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    maxps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    maxps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    maxss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;

procedure SSE2ArrayAxpyF32(aX, aY, aDst: PSingle; aCount: SizeUInt);
var
  pX, pY, pD: PSingle;
begin
  {$PUSH}{$Q-}{$R-}
  if aCount = 0 then Exit;
  pX := aX; pY := aY; pD := aDst;

  asm
    mov rax, pX
    mov rdx, pY
    mov rcx, pD
    mov r8, aCount

    cmp r8, 16
    jb @tail4

  @loop16:
    movups xmm0, [rax]
    mulps xmm0, [rdx]
    movups [rcx], xmm0
    movups xmm1, [rax + 16]
    mulps xmm1, [rdx + 16]
    movups [rcx + 16], xmm1
    movups xmm2, [rax + 32]
    mulps xmm2, [rdx + 32]
    movups [rcx + 32], xmm2
    movups xmm3, [rax + 48]
    mulps xmm3, [rdx + 48]
    movups [rcx + 48], xmm3
    add rax, 64
    add rdx, 64
    add rcx, 64
    sub r8, 16
    cmp r8, 16
    jae @loop16

  @tail4:
    cmp r8, 4
    jb @tail_scalar
    movups xmm0, [rax]
    mulps xmm0, [rdx]
    movups [rcx], xmm0
    add rax, 16
    add rdx, 16
    add rcx, 16
    sub r8, 4
    cmp r8, 4
    jae @tail4

  @tail_scalar:
    test r8, r8
    jz @done
  @scalar_loop:
    movss xmm0, [rax]
    mulss xmm0, [rdx]
    movss [rcx], xmm0
    add rax, 4
    add rdx, 4
    add rcx, 4
    dec r8
    jnz @scalar_loop

  @done:
  end;
  {$POP}
end;
