unit nextpas.core.git.native.history;

{$I nextpas.core.settings.inc}

{ REMOVED — owner converged to shards `traversal/query/ops` single source.
  Tombstone to preserve history; unit contains no code. New code must use
  `nextpas.core.git.native.history.traversal|query|ops` directly. }

interface

implementation

end.
