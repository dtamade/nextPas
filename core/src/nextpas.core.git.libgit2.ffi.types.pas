unit nextpas.core.git.libgit2.ffi.types;
{** @deprecated 2026-09-02: removed — vocabulary single source base←types via nextpas.core.git.libgit2.types (plain helper).
    This shim no longer re-exports libgit2.types and carries no external probe; ffi remains minimal per design-conventions §6.
    New code must use nextpas.core.git.libgit2.types directly. Kept as empty tombstone for BC, will be deleted next minor. *}

{$I nextpas.core.settings.inc}

interface

implementation

end.
