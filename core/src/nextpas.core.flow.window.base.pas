unit nextpas.core.flow.window.base;

{$I nextpas.core.settings.inc}

interface

{ L1 flow.window base seam: pure constants, no logic.
  Keeps four-piece base <- impl <- facade dependency direction. }

const
  FLOW_WINDOW_LOW_WATER_DIVISOR = 2;

implementation

end.
