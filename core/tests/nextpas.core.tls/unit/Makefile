# Complex multi-file test suite — requires manual setup
# Run individual test_*.pas files with run_unit_tests.lpr

.PHONY: build run test clean

build:
	@true

run: build
	@true

test: run
	@echo "  SKIP: tls/unit (complex suite, run manually)"

clean:
	@true
