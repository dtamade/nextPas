PROGRAM := test_http_threaded_wine
# Side suite: Wine/Win64 functional smoke only — not main PROJECTS, not scale-ready.
# Discovered by `make wine-smoke` via *_wine/* path convention.
include ../../common.mk