PROGRAM := test_http_iocp_wine
# Side suite: Wine/Win64 IOCP phase-1 accept + worker handoff smoke.
# Not main PROJECTS, not scale-ready, not real-Windows host evidence.
# Discovered by `make wine-smoke` via *_wine/* path convention.
include ../../common.mk