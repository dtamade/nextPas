PROGRAM := test_http_iocp_wine
# Side suite: IOCP completion recv/send wire smoke (accept + keep-alive +
# writable backpressure). Not main PROJECTS, not scale-ready.
# Discovered by `make wine-smoke` via *_wine/* path convention;
# real-Windows evidence via scripts/http-host-ci-matrix.sh (make test).
include ../../common.mk