PROGRAM := test_http_iocp_facade_wine
# Side suite: product THttpServer facade over tsbIocp (GET + keep-alive).
# The uses clause also gates full-facade Win64 cross-compile. Not main
# PROJECTS, not scale-ready.
# Discovered by `make wine-smoke` via *_wine/* path convention;
# real-Windows evidence via scripts/http-host-ci-matrix.sh (make test).
include ../../common.mk
