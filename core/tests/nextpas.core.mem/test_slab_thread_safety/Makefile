PROGRAM := test_slab_thread_safety
include ../../common.mk

# Preserve historical -dDEBUG build semantics (suite predates common.mk;
# DEBUG ifdefs in the unit under test must stay active).
FPC_FLAGS += -dDEBUG
