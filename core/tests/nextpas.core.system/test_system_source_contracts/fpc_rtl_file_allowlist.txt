# FPC broad RTL file-level allowlist (regenerated 2026-09-04: pruned 100+ stale
# entries other lanes cleaned without pruning; format unchanged
# <Unit>|<relative-path> to keep the gate's grep lookup stable).
# Only live src truth remains:
# KEEP: no owner exists (exception) / system self-zone (typinfo).
# DEFERRED: owned by sibling lanes (winssl.connection -> istream-only IStream-ectomy,
#   winssl.enterprise -> tls lane; touch only with lane-owner sign-off).
# INFRA: deliberate bootstrap bindings, documented in-unit (thread.init cthreads,
#   compress zlib FFI).
SysUtils|nextpas.core.exception.pas
TypInfo|nextpas.core.system.typinfo.pas
Windows|nextpas.core.tls.winssl.connection.pas
Sockets|nextpas.core.tls.winssl.connection.pas
winsock2|nextpas.core.tls.winssl.connection.pas
Registry|nextpas.core.tls.winssl.enterprise.pas
cthreads|nextpas.core.thread.init.pas
zlib|nextpas.core.compress.deflate.pas
zlib|nextpas.core.compress.gzip.pas
zlib|nextpas.core.compress.zlib.ffi.pas
