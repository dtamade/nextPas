# D2 · M0 真值轻启动状态（2026-07-23）

> 记录 D1 path-limited landing（B25–B29）完成后的 D2 轻量审计，不替代
> `docs/superpowers/plans/2026-07-11-compiler-system-m0-truth-convergence.md`。

## 当前绿项

| Gate | 结果 |
| --- | --- |
| `make system-projection-check` | green（linux-x86_64 ready） |
| system projection source-contract 子检查 | green（`check_system_projection.sh`） |
| B25–B29 focused gates | 已随 D1 进 main 且绿 |
| `compiler-pass` | 53/53（D1 候选上） |

## 明显断裂（未在本回合修）

| 断裂 | 证据 | 建议处置 |
| --- | --- | --- |
| **sysutils facade public surface drifted** | `make -C core/tests/nextpas.core.system/test_system_source_contracts test` → `[FAIL] sysutils facade public surface drifted`：expected 约 50 符号，actual 仅 `Exception` / `ExceptClass` | 优先查 `facade_parser.sh` / `list_unit_facade_surface` 是否在 type alias 后提前截断；`core/src/nextpas.core.system.sysutils.pas` interface 仍声明 Format/IntToStr/… 全表面，更像解析器问题而非 facade 被清空 |
| **`make landing-check` 附带 tooling 红点** | `test_focused_gate_matrix_doc_contract.sh`：`missing focused gate matrix doc contract: mem lane usability guardrails gate command` | **与 compiler-system 无关**；main 上已红，勿塞进本 lane 修复 |

## D2 M0 完整任务清单（下一拍）

1. **修 source-contract 解析断裂**：让 sysutils（及同类 facade）surface allowlist 与真实 interface 对齐；先绿 `test_system_source_contracts`。
2. **coverage table / ledger 对账**：对照 `core/docs/system/contract-coverage-table.md` 与 typed ledger（plan 中的 IR ledger）；缺项/多余/乱序由 source-contract 拒收。
3. **文档 claim 收敛**（小范围）：`core/docs/system/README.md` / `goal-tree.md` / readiness 去掉无 gate 的 “complete / production ready” 说法；只写当前 evidence 边界。
4. **不在 M0 做的**（plan 已写死）：
   - 不迁 sema/HIR 热路径到 typed ledger 分发
   - 不加 fingerprint / semantic snapshot
   - 不改 resolver 依赖成员 / 平台排除
   - 不碰 http/mem/platform/simd 实现
5. **验证包**（每步后）：
   - `make system-projection-check`
   - `make -C core/tests/nextpas.core.system/test_system_source_contracts test`
   - 相关 focused compiler gates（若动 IR/sema）
   - `make hygiene` + `git diff --check`
6. **落地纪律**：继续 path-limited landing，禁止 raw-merge `codex/compiler-system`。

## 相对 M0 plan 的进度判断

- Projection 路径与 check 已绿 → plan 中 “installed System projection” 侧可用。
- source-contract 仍因 facade surface 解析失败整门红 → **M0 文档/ledger 对账不应在 gate 红时宣称完成**。
- B25–B29 是 L1 编译器可执行证据 slice，已进 main；与 M0 文档真值是正交轨道，可并行但不要混 landing。

## 本回合边界

- D1 Landed；D2 仅交付本状态 + 清单。
- 未改 production 代码、未造 `task_plan.md`。