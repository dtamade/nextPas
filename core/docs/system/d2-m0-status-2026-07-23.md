# D2 · M0 真值状态（2026-07-23）

> 记录 D1 path-limited landing（B25–B29）完成后的 D2 M0 真值推进。
> 不替代 `docs/superpowers/plans/2026-07-11-compiler-system-m0-truth-convergence.md`。

## Landing 状态

**D2 path-limited landing → main 已完成**（2026-07-23，ff-only，未 push）。

| 项 | 值 |
| --- | --- |
| main HEAD（合入后） | `ee6580baa` |
| 合入 commits | `4e65eccfa` docs light-start · `2b08626ab` source-contract fix · `ee6580baa` status after green |
| 路径 | `core/docs/system/d2-m0-status-…` · `core/src/nextpas.core.system.sysutils.pas` · `test_system_source_contracts/*` |
| 方式 | 临时 `landing/compiler-system-d2-20260723` cherry-pick，**禁止** raw-merge `codex/compiler-system` |
| `landing-candidate` | pass |
| `landing-check` tooling | 红：`test_focused_gate_matrix_doc_contract`（mem lane，与 D2 无关）— 按 D1 先例仍 ff-only |

## 当前绿项

| Gate | 结果 |
| --- | --- |
| `make system-projection-check` | green（linux-x86_64 ready） |
| `make -C …/test_system_source_contracts test` | green（projection + source contracts） |
| `make -C …/test_system_contracts clean test` | green（5/5 ledger tests） |
| `test_system_sysutils_minimal` | green（6/6，landing 候选验证） |
| contracts vocabulary ↔ typed ledger | 1:1（`NPSYSTEM_*` 常量集一致） |
| coverage table ↔ `np.system.*` names | 1:1（无缺项/多余） |
| B25–B29 focused gates | 已随 D1 进 main |

## 根因与修复（已 land）

### 1. `sysutils facade public surface drifted` — **期望过时，非解析截断**

**修复**：更新 `require_sysutils_facade_surface_allowlist` expected 与真实 interface 对齐。

### 2. `system unit bypasses platform owner: … uses BaseUnix`

**修复**：`GetProcessID` 去掉 BaseUnix/Windows，委派 FPC `System.GetProcessID`。

### 3. FPC RTL file-level debt：`net.async.udp` 用 SysUtils

**修复**：allowlist 记命名 debt（baseline 诚实）。**不**在本 lane 改 net 代码。

## 相对 M0 plan 的进度

| Plan 块 | 状态 |
| --- | --- |
| Task1 projection | 已绿 |
| Task2 typed ledger | 已绿（5/5） |
| Task3 source-contract / logical owner | **已绿并 land** |
| Task4 文档 authority tokens | 已绿（README/goal-tree/readiness 含诚实边界） |
| 热路径迁 typed ledger | **不做**（plan 禁止） |
| A→B→C / M2-A | **不做** |

## 文档 claim

权威边界（source-contract 锁定）：

- M0 truth convergence in progress；not self-host ready
- A → B → C has not executed
- `process_init`/`process_fini`：compiler semantic live；**runtime execution deferred**（coverage / README）
- `unit_init`/`unit_fini`：**future compiler/runtime only**
- Historical S* completion 非当前 readiness authority

本轮未再发现需删的虚假「已自举/生产就绪」权威 claim。

## D3 入口审计（文档 + 事实对齐；见下节 D3 窄切片）

### typed ledger 入口（`compiler/ir/np_system_contracts.pas`）

| Kind | SemanticName | EvidenceLevel | FocusedEvidence | RuntimeMapping |
| --- | --- | --- | --- | --- |
| `sckProcessInit` | `np.system.process_init` | **scelHir** | `test_process_lifecycle` (+ llvm) | `np_process_init` |
| `sckProcessFini` | `np.system.process_fini` | **scelHir** | `test_process_lifecycle` (+ llvm) | `np_process_fini` |
| `sckUnitInit` | `np.system.unit_init` | **scelSemantic** | `test_semantic_runtime_contract_seed` | unit-specific init entry |
| `sckUnitFini` | `np.system.unit_fini` | **scelSemantic** | `test_semantic_runtime_contract_seed` | unit-specific fini entry |

### 代码侧已有（不构成 readiness）

- 编译器：seed `process_init`/`process_fini`；生成 `np_unit_init_*`/`np_unit_fini_*`；`_start` 侧 call 序列
- 运行时 Phase 0：`rtl/runtime/src/nextpas.runtime.lifecycle.ll` — 状态标志 + fsync；**不做** unit 表 / 堆 / ExitProc
- 权威仍 **deferred / future**；ledger 未升到 `scelExecutable`

### 历史文档矛盾（已修补）

`self-hosting-readiness.md` Gate 2 residual 与 coverage 权威对齐；**PHASE 0 COMPLETE 为 historical assessment**。

## D3 窄切片（2026-07-23）— process lifecycle HIR/LLVM 证据加固

**范围**：process_init/process_fini focused 再验 + 最小发射修复 + coverage 诚实脚注。**未**抬 ledger。

| 项 | 结果 |
| --- | --- |
| `test_process_lifecycle` | green（exit 0） |
| `test_process_lifecycle_llvm` | green（exit 0；修复前 red：`call i64` + 双 `process_fini`） |
| IR 形状 | `call void @np_process_init()` → `call void @np_process_fini()` → halt；declare 均为 void |
| ledger | 仍 **scelHir**；coverage 仍 **Runtime execution deferred** |
| 生产改动 | `EmitProcessInit/Fini` ResultId=0；emitter HIR `np_process_fini` 路径设 `FProcessFiniEmitted` |

**根因**：void 生命周期 call 误设 ResultId → `EmitCallInstr` 把 void 升级为 `call i64`；`process_fini` 发射后未标记，halt inject 再发一次。

**诚实边界**：HIR/LLVM call 证据 ≠ 全量 runtime business init；≠ host-free 端到端；≠ A→B→C / self-host。

## 仍 open / 下一刀清单

1. **FPC debt（net lane）**：`net.async.udp` 迁出 SysUtils 后收紧 allowlist。
2. **D3 后续（可选）**：unit_init/unit_fini multi-unit **ordering** 仍无 executable 证明——仅脚注/负向说明，不抬「已实现」。
3. **typed 热路径迁移**：**不做**。
4. **A→B→C / M2-A**：**不做**。
5. **无关红点**：历史 mem matrix 文档合同（docs-only landing 未踩）。

## 禁止

- raw-merge `codex/compiler-system`
- 一次做完整 typed 大迁移
- 把 Historical S10/S11 或 PHASE 0 勾选当作当前 readiness
- 因 HIR/LLVM call 绿而抬 `scelExecutable` / 声称 process lifecycle 生产完成
