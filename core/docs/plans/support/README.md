# Core Plan Support Records

This directory stores completed control logs, findings, and progress notes that support plans in `core/docs/plans/`.

Use the dated files here for historical evidence. Do not treat them as the current active task list.
