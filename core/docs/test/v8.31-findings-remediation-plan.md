# v8.31 Findings 修复方案（完整规划）

**依据**: `core/docs/test/findings.md`（2026-07-26 审计）  
**目标版本**: **v8.31** — findings 全量 remediation  
**模式**: 规划 → 实施 → 验证（本 slice 一次落地）

---

## 1. 范围与非目标

| 做 | 不做 |
|----|------|
| 全部 P1（F-01,05,06,07,13,15,16） | SoftExpect 全镜像（F-17 v9） |
| 可安全落地的 P2/P3 | TSAN 一体化（F-18 工具链阻塞） |
| 文档/契约单源化 | 跨 lane 扫 TLS 手写 Assert（F-24） |
| 轻量 prop 可维护性（RNG 线程安全） | prop.pas 全文件拆成 5+ 单元（过大，见 F-03 分期） |

---

## 2. 架构变更

```
L0: base, config, diff          ← ColorDiff 下沉到 test.diff（破 L1→L2）
L1: check, expect, snapshot     ← CheckSnapshot 共享单元
L2: output*                     ← ColorDiff re-export from diff；泄漏探针可注入
L3: runner*
L4: discovery, mock, prop, helpers, bench
门面: test.pas re-export bench 公共 API + 文档白名单
```

---

## 3. 任务映射

| ID | 动作 | 验收 |
|----|------|------|
| F-01 | 新建 `test.diff`；check 不再 uses output | 分层图无 L1→L2 |
| F-02 | 新建 `test.snapshot`；expect 不 uses check | expect uses snapshot/diff only |
| F-05/11 | 去掉 GetFPCHeapStatus；可注入 probe + delta | 默认无 FPC heap 符号 |
| F-06 | Coverage 警告走 ErrSink | 无 WriteLn(StdErr) |
| F-07 | FuzzParallel 仅 deprecated 转发 + 文档诚实 | CONTRACT/README 标明 sequential |
| F-08 | GFuzzRng threadvar + 直接方法调用（修 record 拷贝 bug） | 文档非线程安全→per-thread RNG |
| F-09 | CONTRACT 固化 stuck detach 语义 | 文档 |
| F-10 | GetCallHistory CheckThread | mock 测一行 |
| F-12 | runner source-contract：Add 后再 Test 的 COW 警告注释/测 | 契约测或文档 |
| F-13 | scale 增加 MIN_NON_TABLE；thin 套件 fail-path | scale 门过 |
| F-14/15/16 | 文档对齐 Double=Near；contracts stub | 无矛盾语义 |
| F-19 | Discovery 固定名表 stub 测加厚 | discovery 绿 |
| F-20 | Parallel+TestSubtest 注册期配置错误 | parallel 测 |
| F-03 | 分期：本版只 RNG/文档；大拆分记 backlog | backlog |
| F-04 | facade 说明 + 可选 re-export bench 类型 | 文档/门面 |
| F-21/22/25 | 文档索引与 CLI 说明 | 文档 |

---

## 4. 验证矩阵

```bash
make hygiene
make -C core/tests/nextpas.core.test contracts
make focused FOCUS=core/tests/nextpas.core.test/lane_gate
make -C core/tests/nextpas.core.test/test_assertions clean test
make -C core/tests/nextpas.core.test/test_expect clean test
make -C core/tests/nextpas.core.test/test_output clean test
make -C core/tests/nextpas.core.test/test_prop clean test
make -C core/tests/nextpas.core.test/test_mock clean test
make -C core/tests/nextpas.core.test/test_parallel clean test
make -C core/tests/nextpas.core.test/test_discovery clean test
make -C core/tests/nextpas.core.test/test_advanced clean test
make -C core/tests/nextpas.core.test/test_bench clean test
make -C core/examples/nextpas.core.test/table_driven_demo run
# RTL 边角
rg -n 'GetFPCHeapStatus|WriteLn\(StdErr' core/src/nextpas.core.test*.pas  # 期望 0
rg -n 'nextpas\.core\.test\.output' core/src/nextpas.core.test.check.pas   # 期望 0
```

---

## 5. Landing

path-limited：`core/src/nextpas.core.test*`、`core/tests/nextpas.core.test/**`、`core/docs/test/**`、`docs/contracts/test.md`、`core/examples/nextpas.core.test/**`（若有）。
