// Intentional empty include:
// permanently-scalar NEON platform facade helpers were removed after the
// dispatch table started reusing the canonical base scalar slots directly.
// Keep this file as an audited include boundary so docs/tests can point at the
// retired facade layer without reintroducing backend-local wrappers.
