unit np_compilation_session;

{$mode objfpc}{$H+}

interface

uses
  nextpas.compiler.frontend.session;

type
  TBuildContext = nextpas.compiler.frontend.session.TBuildContext;
  TCompilationOptions = nextpas.compiler.frontend.session.TCompilationOptions;
  TToolStatusEventRecord = nextpas.compiler.frontend.session.TToolStatusEventRecord;
  TToolStatusEventVec = nextpas.compiler.frontend.session.TToolStatusEventVec;
  TCompilationSession = nextpas.compiler.frontend.session.TCompilationSession;

implementation

end.