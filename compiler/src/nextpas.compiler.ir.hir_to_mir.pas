unit nextpas.compiler.ir.hir_to_mir;

{$mode objfpc}{$H+}

interface

uses
  nextpas.compiler.ir.hir.to_mir;

type
  THirMirValueMapEntry = nextpas.compiler.ir.hir.to_mir.THirMirValueMapEntry;
  THirMirValueMap = nextpas.compiler.ir.hir.to_mir.THirMirValueMap;
  THirToMirLowering = nextpas.compiler.ir.hir.to_mir.THirToMirLowering;

implementation

end.
