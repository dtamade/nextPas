unit nextpas.compiler.ir.mir_model;

{$mode objfpc}{$H+}

interface

uses
  np_mir_model;

type
  TMirValueId = np_mir_model.TMirValueId;
  TMirBlockId = np_mir_model.TMirBlockId;
  TMirBlockIdArray = np_mir_model.TMirBlockIdArray;
  TMirStructField = np_mir_model.TMirStructField;
  TMirStructFieldVec = np_mir_model.TMirStructFieldVec;
  TMirStructType = np_mir_model.TMirStructType;
  TMirFuncId = np_mir_model.TMirFuncId;
  TMirOperandKind = np_mir_model.TMirOperandKind;
  TMirConst = np_mir_model.TMirConst;
  TMirOperand = np_mir_model.TMirOperand;
  TMirStmtKind = np_mir_model.TMirStmtKind;
  TMirOp = np_mir_model.TMirOp;
  TMirOperandVec = np_mir_model.TMirOperandVec;
  TMirStmt = np_mir_model.TMirStmt;
  TMirTermKind = np_mir_model.TMirTermKind;
  TMirSwitchCase = np_mir_model.TMirSwitchCase;
  TMirSwitchCaseVec = np_mir_model.TMirSwitchCaseVec;
  TMirTerminator = np_mir_model.TMirTerminator;
  TMirStmtVec = np_mir_model.TMirStmtVec;
  TMirBlock = np_mir_model.TMirBlock;
  TMirParam = np_mir_model.TMirParam;
  TMirParamVec = np_mir_model.TMirParamVec;
  TMirBlockVec = np_mir_model.TMirBlockVec;
  TMirFunction = np_mir_model.TMirFunction;
  TMirFunctionVec = np_mir_model.TMirFunctionVec;
  TMirStructTypeVec = np_mir_model.TMirStructTypeVec;
  TMirModule = np_mir_model.TMirModule;

implementation

end.
