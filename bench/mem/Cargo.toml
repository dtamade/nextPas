[package]
name = "mem_bench"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "mem_bench"
harness = false

[dependencies]
crossbeam-queue = "0.3"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
