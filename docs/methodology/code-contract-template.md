# 代码契约模板

> 使用说明：复制此模板到 `core/docs/<module>/CONTRACT.md`，按实际情况填写。
> 带 `[ ]` 的是检查项，填写后改为 `[x]`。

---

# [模块名] 代码契约

**模块路径**：`core/src/nextpas.core.<module>*.pas`
**层级**：L0 / L1 / L2 / L3
**Owner**：[负责 AI / 开发者]
**最后更新**：2026-08-31 <!-- 示例：2026-08-31 -->

---

## 1. 接口契约

### 1.1 公开类型

| 类型 | 所在单元 | 职责 | 关键不变量 |
|------|----------|------|------------|
| `TXxx` | `nextpas.core.xxx.pas` | ... | ... |

### 1.2 公开函数

#### `TXxx.MethodName(params): ReturnType`

**前置条件**：
- `param1` 必须非 nil
- `param2` 必须 > 0

**后置条件**：
- 返回值非 nil（成功时）
- 返回值 nil（失败时，不抛异常）

**副作用**：
- 修改内部状态 X
- 无副作用

**异常**：
- 不抛异常 / 抛出 `EXxx`（条件）

#### `FunctionName(params): ReturnType`

**前置条件**：...
**后置条件**：...
**副作用**：...
**异常**：...

---

## 2. 不变量

> 以下条件在任何公开方法调用前后都必须为真。

- [ ] **[INV-1]** `FAllocator <> nil`（构造时通过 ResolveAllocator 保证）
- [ ] **[INV-2]** `FOffset <= FCapacity`
- [ ] **[INV-3]** ...

---

## 3. 错误处理

### 3.1 异常层级

```
Exception
  └── EAllocError (aeXxx: TAllocError)
       ├── EOutOfMemory (aeOutOfMemory)
       ├── EInvalidLayout (aeInvalidLayout)
       └── EDoubleFree (aeDoubleFree)
```

### 3.2 错误码

| 错误码 | 含义 | 使用场景 |
|--------|------|----------|
| `aeOutOfMemory` | 内存不足 | GetMem/AllocMem 返回 nil 后抛出 |
| `aeInvalidLayout` | 布局/大小非法 | 块大小溢出、对齐不满足 |
| ... | ... | ... |

### 3.3 Graceful degradation

- [ ] OOM 场景：返回 nil 还是抛异常？（明确策略）
- [ ] 非法参数：抛异常还是返回错误码？
- [ ] 资源耗尽：降级还是失败？

---

## 4. 线程安全

### 4.1 并发模型

| 类型 | 线程安全 | 机制 |
|------|----------|------|
| `TXxx` | ✅ / ❌ | TLS / 单锁 / 无锁 / 非线程安全 |

### 4.2 锁策略

- [ ] 锁顺序：`FLock` → 无嵌套
- [ ] 锁粒度：方法级 / 字段级
- [ ] 死锁风险：无 / 需注意 X

### 4.3 无锁保证

- [ ] CAS 操作用于 X
- [ ] 原子操作用于 Y
- [ ] 内存序：acquire/release/seq_cst

---

## 5. 内存管理

### 5.1 所有权模型

- [ ] 谁分配谁释放
- [ ] 所有权转移：通过返回值 / 通过参数
- [ ] 共享所有权：引用计数 / GC

### 5.2 生命周期

```
Create → Use → Destroy
  ↑                ↓
  └── FAllocator 生命周期绑定 ──┘
```

### 5.3 Leak-free 保证

- [ ] heaptrc 测试覆盖
- [ ] 异常路径不会泄漏
- [ ] Destroy 释放所有内部资源

---

## 6. 测试覆盖

### 6.1 必须覆盖的场景

| 场景 | 测试文件 | 状态 |
|------|----------|------|
| 正常分配/释放 | `test_xxx.lpr` | [ ] |
| OOM 降级 | `test_oom.lpr` | [ ] |
| 对齐验证 | `test_xxx.lpr` | [ ] |
| 双重释放检测 | `test_xxx.lpr` | [ ] |
| 线程安全 | `test_concurrent.lpr` | [ ] |
| 边界条件（0 大小、最大大小） | `test_xxx.lpr` | [ ] |

### 6.2 测试矩阵

| 子系统 | 测试文件 | 套件数 | 测试数 | 失败数 |
|--------|----------|--------|--------|--------|
| ... | ... | ... | ... | 0 |

### 6.3 覆盖率目标

- [ ] 公开 API：100% 调用覆盖
- [ ] 错误路径：100% 触发覆盖
- [ ] 边界条件：核心场景覆盖

---

## 变更记录

| 日期 | 版本 | 变更描述 | 作者 |
|------|------|----------|------|
| 2026-08-31 | 1.0 | 初始版本 | ... | <!-- 示例：2026-08-31 首次填写时使用当日日期 --> |
